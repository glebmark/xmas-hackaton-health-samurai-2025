# Aidbox Configuration
# Use the BASE URL only (without /ui/console or other paths)
# Example: https://your-instance.aidbox.app (NOT https://your-instance.aidbox.app/ui/console)
AIDBOX_URL=https://your-aidbox-instance.aidbox.app

# Client credentials for OAuth (client_credentials grant type)
# Create a Client in Aidbox with:
#   PUT /Client/api-client
#   secret: your-secret
#   grant_types: [client_credentials]
# And an AccessPolicy with engine: allow linked to this Client
AIDBOX_CLIENT_ID=api-client
AIDBOX_CLIENT_SECRET=verysecret

AIDBOX_USER_AUTH_CLIENT_ID=user-auth-client
AIDBOX_USER_AUTH_CLIENT_SECRET=<the-secret>

# Server Configuration
PORT=3001
